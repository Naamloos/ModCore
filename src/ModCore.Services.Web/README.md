# ModCore.Services.Web
This service handles ModCore's Web-based service.

# Debugging
To debug ModCore.Services.Web, You'll also want to run `npm run dev` in the `ClientApp` directory. If this fails, run `npm i` in the `ClientApp` directory first.